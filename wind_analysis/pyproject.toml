[project]
name = "anemo-wind-analysis"
version = "0.1.0"
description = "Render wind data stored in ANEMO PostgreSQL as a Folium map"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "folium==0.19.5",
    "polars==1.26.0",
    "psycopg2-binary==2.9.12",
]

[tool.uv]
package = false
