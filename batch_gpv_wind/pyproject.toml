[project]
name = "anemo-batch-gpv-wind"
version = "0.1.0"
description = "Import JMA MSM GRIB2 wind data into the ANEMO PostgreSQL database"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "numpy==2.2.4",
    "pandas==2.2.3",
    "psycopg2-binary==2.9.12",
    "pygrib==2.1.6",
]

[tool.uv]
package = false
