{
  "name": "wind_generator",
  "version": "1.1.0",
  "description": "ANEMO wind-field editor and optional PostgreSQL API",
  "main": "dist/server/server.js",
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "build": "npm run clean && tsc -p tsconfig.server.json && tsc -p tsconfig.client.json",
    "check": "tsc -p tsconfig.server.json --noEmit && tsc -p tsconfig.client.json --noEmit && tsc -p tsconfig.test.json --noEmit",
    "test": "tsc -p tsconfig.test.json && node --test dist/test/tests/wind-field.test.js",
    "start": "npm run build && node dist/server/server.js",
    "start:built": "node dist/server/server.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "leaflet": "^1.9.4",
    "pg": "^8.14.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/leaflet": "^1.9.17",
    "@types/node": "^22.13.14",
    "@types/pg": "^8.11.11",
    "typescript": "^4.0.0"
  },
  "author": "Daiki Shibata",
  "license": "ISC"
}
